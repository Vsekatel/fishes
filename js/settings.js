
const MAX_SPEED = new Vector(10, 10);

// ---------------------------------------------------------------------------

const TEXT_STYLE_ID = new PIXI.TextStyle({
	fontFamily: 'Arial',
	fontSize: 12,
	fontWeight: 'bold',
	stroke: '#d742f5',
	strokeThickness: 5,
	dropShadow: false,
	wordWrap: true,
	wordWrapWidth: 440,
	lineJoin: 'round',
});